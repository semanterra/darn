{
    "private": true,
    "workspaces": [
        "packages/somelib",
        "packages/someclient"
    ],
    "scripts": {
        "bootstrap": "lerna bootstrap",
        "test": "lerna run test",
        "prepare": "lerna run prepare",
        "clean": "rimraf packages/**/lib"
    },
    "devDependencies": {
        "lerna": "^3.0.4",
        "rimraf": "^2.6.2",
        "typescript": "^3.0.1"
    }
}
